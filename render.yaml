services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python your_bot_file.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8